[project]
name = "django-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2.7",
    "ruff>=0.14.0",
]

[tool.ruff]
exclude = ["migrations/*.py"]
